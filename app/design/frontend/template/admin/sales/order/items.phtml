<?php $items = $this->getItems(); ?>
<div class="container mt-4">
    <div class="card p-4">
        <h4 class="mb-3 fw-bold">Order Review Summary</h4>
        <div class="table-responsive">
            <table class="table table-bordered align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Product</th>
                        <th>Brand</th>
                        <th>Material</th>
                        <th>Price</th>
                        <th>Quantity</th>
                        <th>Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($this->getItems() as $item): ?>
                        <?php $product = $item->getProductData(); ?>
                        <tr>
                            <td class="d-flex align-items-center">
                                <img src="<?php echo $this->getImageUrl($product->getImages()['coverImage']) ?>" 
                                    alt="Product Image" class="img-fluid rounded me-2" style="width: 50px; height: 50px;">
                                <span><?php echo $product->getName(); ?></span>
                            </td>
                            <td><?php echo $product->getBrand(); ?></td>
                            <td><?php echo $product->getMaterial(); ?></td>
                            <td class="text-primary fw-bold">₹<?php echo number_format($product->getPrice(), 2); ?></td>
                            <td><?php echo $item->getProductQuantity(); ?></td>
                            <td class="fw-semibold">₹<?php echo number_format($item->getSubTotal(), 2); ?></td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
