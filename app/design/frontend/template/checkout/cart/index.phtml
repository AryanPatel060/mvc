<!-- <h1>index</h1> -->

<div class="container mt-5">
    <h2 class="text-center mb-4">Shopping Cart</h2>

    <table class="table table-bordered">
        <thead class="table-primary">
            <tr>
                <th>Product</th>
                <th>Name</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Total</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <!-- Loop through cart data -->
            <?php foreach ($this->getCartProductData() as $product): ?>
                <tr>
                    <td><img src="<?php //$this->getIamgeUrl($product->getProductID()); ?>" class="img-fluid" alt="Product"></td>
                    <td><?= $product->getName() ?></td>
                    <td><?= $product->getPrice() ?></td>
                    <td>
                        <div class="d-flex align-items-center">
                            <button class="btn btn-sm btn-outline-primary">-</button>
                            <input type="text" class="form-control text-center mx-2" value="1" style="width: 50px;">
                            <button class="btn btn-sm btn-outline-primary">+</button>
                        </div>
                    </td>
                    <td><?= $product->getPrice() ?></td>
                    <td><a href="<?= $this->getUrl("*/*/remove") . '/?productid=' . $product->getProductId() ?>" role="button" class="btn btn-sm btn-danger">Remove</a></td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <h4>Total: <span>$250</span></h4>
        <button class="btn btn-success">Proceed to Checkout</button>
    </div>
</div>