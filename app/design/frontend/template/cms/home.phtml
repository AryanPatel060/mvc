<style>
    /* Ensure the Swiper container takes full width */
    .swiper {
        width: 100%;
        height: 100vh; 
        display: block;/* Full viewport height */
    }

    /* Each slide container */
    .swiper-slide {
        display: flex;
        align-items: center;
        justify-content: center;
        /* background: #f8f9fa; */
        padding: 20px;
        border-radius: 10px;
    }

    /* Grid layout: Text on the left, Image on the right */
    .swiper-slide-content {
        display: flex;
        width: 100%;
        max-width: 900px;
        align-items: center;
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Left side: Text content */
    .product-info {
        flex: 1;
        padding: 20px;
        text-align: left;
    }

    .product-info h2 {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    .desc {
        font-size: 16px;
        color: #666;
    }

    /* Right side: Image */
    .product-image {
        flex: 1;
        text-align: center;
    }

    .product-image img {
        max-width: 100%;
        height: auto;
        border-radius: 8px;
    }

    /* Swiper navigation */
    .swiper-button-next,
    .swiper-button-prev {
        color: black;
    }
    .btnfade{
        color: rgba(0, 0,0,0.1 );
    }
</style>
<div class="swiper mySwiper">
    <div class="swiper-wrapper">
        <?php foreach($this->getBannerProduct() as $product): ?>
        <div class="swiper-slide">
            <div class="swiper-slide-content">
                <div class="product-info">
                    <h2><?= $product->getName(); ?></h2>
                    <p class="desc"><?= $product->getDescription(); ?></p>
                </div>
                <div class="product-image">
                    <img src="<?= $this->getImageUrl($this->getCoverImage($product->getProductId())) ?>" alt="<?= $product->getName(); ?>">
                </div>
            </div>
        </div>
        <?php endforeach; ?>
    </div>

    <!-- Swiper Pagination & Navigation -->
    <div class="swiper-pagination"></div>
    <div class="swiper-button-next btnfade"></div>
    <div class="swiper-button-prev btnfade"></div>
</div>

<!-- Swiper.js & Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<script>
    var swiper = new Swiper(".mySwiper", {
        loop: true, // Infinite loop
        autoplay: {
            delay: 3000,
            disableOnInteraction: false
        },
        pagination: {
            el: ".swiper-pagination",
            clickable: true
        },
        navigation: {
            nextEl: ".swiper-button-next",
            prevEl: ".swiper-button-prev"
        }
    });
</script>
